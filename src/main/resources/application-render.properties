# This file contains properties specific to the Render deployment environment

# Use the PostgreSQL dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# The datasource URL is provided by Render via an environment variable
# No need to set spring.datasource.url, username, or password here
